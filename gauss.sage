class GaussianRational:
    def __init__(self,x,y):
        self.x = QQ(x)
        self.y = QQ(y)
    def __add__(self,right)
        self.y += right.y
        self.x += right.x
        
    def __subtract__(self,right):
        """
        docstring on github
        """
        return 'booyah!'
